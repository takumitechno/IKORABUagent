import { escapeHtml } from "../components/layout";
import {
  customerDashboardStyles, customerThemeScript, dashboardIcon,
} from "../components/customer-dashboard-visuals";
import type { ThreadsContent, ThreadsDashboardData, ThreadsMetricKey } from "../lib/threads-dashboard";

const metricLabels: Partial<Record<ThreadsMetricKey, string>> = {
  views: "表示", likes: "いいね", replies: "返信", reposts: "再投稿", quotes: "引用", shares: "シェア",
};

function fmtDate(value: string | null, fallback = "取得不可"): string {
  if (!value) return fallback;
  const date = new Date(value);
  if (Number.isNaN(date.getTime())) return fallback;
  return new Intl.DateTimeFormat("ja-JP", {
    month: "numeric", day: "numeric", hour: "2-digit", minute: "2-digit", timeZone: "Asia/Tokyo",
  }).format(date);
}

function roleLabel(role: string | null): string {
  return ({ reach: "認知", trust: "信頼", desire: "興味", conversion: "行動" } as Record<string, string>)[role || ""] || "未分類";
}

function originLabel(origin: ThreadsContent["origin"]): string {
  return ({ ai_auto: "AI自動", ai_manual: "AI手動", human_manual: "人手投稿", unknown: "作成方法不明" })[origin];
}

function hasMetrics(content: ThreadsContent): boolean {
  return Object.values(content.metrics).some((value) => typeof value === "number");
}

function valueOf(content: ThreadsContent, key: ThreadsMetricKey): string {
  const value = content.metrics[key];
  return typeof value === "number" ? value.toLocaleString("ja-JP") : "取得不可";
}

function ratio(content: ThreadsContent, key: "likes" | "replies"): string {
  const views = content.metrics.views, numerator = content.metrics[key];
  if (typeof views !== "number" || typeof numerator !== "number" || views <= 0) return "取得不可";
  return `${((numerator / views) * 100).toFixed(2)}%`;
}

function customerText(summary: Record<string, unknown> | null, keys: string[]): string | null {
  if (!summary) return null;
  for (const key of keys) {
    const value = summary[key];
    if (typeof value === "string" && value.trim()) return value.trim().slice(0, 240);
  }
  return null;
}

function renderMetricChip(content: ThreadsContent, key: ThreadsMetricKey): string {
  const value = content.metrics[key];
  const available = typeof value === "number";
  return `<span class="fact-chip ${available ? "" : "missing"}"><small>${metricLabels[key] || key}</small><b>${available ? value.toLocaleString("ja-JP") : "取得不可"}</b></span>`;
}

function renderComparisonBars(contents: ThreadsContent[]): string {
  if (contents.length < 2) {
    return `<div class="report-empty">${dashboardIcon("chart")}<div><b>比較できる投稿がまだありません</b><p>KPIを取得した投稿が2件以上になると、同じ尺度で比較します。</p></div></div>`;
  }
  const definitions: Array<{ label: string; value: (content: ThreadsContent) => number | null; format: (value: number) => string }> = [
    { label: "表示", value: (c) => c.metrics.views ?? null, format: (v) => v.toLocaleString("ja-JP") },
    { label: "表示/時", value: (c) => c.viewsPerHour, format: (v) => v.toFixed(1) },
    { label: "いいね率", value: (c) => typeof c.metrics.views === "number" && c.metrics.views > 0 && typeof c.metrics.likes === "number" ? (c.metrics.likes / c.metrics.views) * 100 : null, format: (v) => `${v.toFixed(2)}%` },
    { label: "返信率", value: (c) => typeof c.metrics.views === "number" && c.metrics.views > 0 && typeof c.metrics.replies === "number" ? (c.metrics.replies / c.metrics.views) * 100 : null, format: (v) => `${v.toFixed(2)}%` },
    { label: "反応/時", value: (c) => c.engagementPerHour, format: (v) => v.toFixed(1) },
  ];
  return `<div class="comparison-bars">${definitions.map((definition) => {
    const values = contents.map((content) => definition.value(content));
    const available = values.filter((value): value is number => typeof value === "number");
    const max = Math.max(1, ...available);
    return `<div class="comparison-group"><strong>${definition.label}</strong><div>${contents.map((content, index) => {
      const value = values[index];
      return `<div class="comparison-line"><span title="${escapeHtml(content.topic || "テーマ未設定")}">${escapeHtml(content.topic || "テーマ未設定")}</span><i><em style="width:${typeof value === "number" ? Math.max(value > 0 ? 2 : 0, value / max * 100).toFixed(1) : 0}%"></em></i><b>${typeof value === "number" ? definition.format(value) : "取得不可"}</b></div>`;
    }).join("")}</div></div>`;
  }).join("")}</div>`;
}

function renderHypotheses(measured: ThreadsContent[]): string {
  if (!measured.length) return `<div class="report-empty compact"><div><b>仮説を組み立てる実測値がありません</b><p>数値が届くまで、結果を推測しません。</p></div></div>`;
  const totalViews = measured.reduce((sum, content) => sum + (content.metrics.views ?? 0), 0);
  const replyKnown = measured.filter((content) => typeof content.metrics.replies === "number");
  const totalReplies = replyKnown.reduce((sum, content) => sum + (content.metrics.replies ?? 0), 0);
  const hypotheses = [{
    title: totalViews > 0 && replyKnown.length && totalReplies === 0
      ? "閲覧後の返信につながる余白が弱い可能性"
      : "反応の違いを生む要素はまだ特定できない",
    evidence: replyKnown.length ? `表示 ${totalViews.toLocaleString("ja-JP")} / 返信 ${totalReplies.toLocaleString("ja-JP")}（取得済み投稿の合計）` : `表示データ ${measured.length}件、返信は取得不可を含みます`,
    counter: "同じ条件の投稿で返信が継続して得られれば、この仮説は弱まります。",
    missing: "同一テーマ・近い時間帯で、末尾だけを変えた比較",
  }];
  if (measured.filter((content) => content.viewsPerHour !== null).length >= 2) hypotheses.push({
    title: "初速には差がある可能性",
    evidence: "公開から観測までの時間で正規化した表示/時に差があります。",
    counter: "観測時点を揃えた追加投稿で差が再現しなければ、この仮説は弱まります。",
    missing: "同じ観測窓と、投稿時間帯を揃えた追加サンプル",
  });
  return `<div class="hypothesis-list">${hypotheses.slice(0, 3).map((item, index) => `<article class="hypothesis-card"><div class="hypothesis-head"><span>${index + 1}</span><div><small>仮説</small><h3>${escapeHtml(item.title)}</h3></div><em>根拠 ${measured.length >= 3 ? "限定的" : "初期"}</em></div><dl><div><dt>根拠データ</dt><dd>${escapeHtml(item.evidence)}</dd></div><div><dt>反証条件</dt><dd>${escapeHtml(item.counter)}</dd></div><div><dt>不足データ</dt><dd>${escapeHtml(item.missing)}</dd></div></dl></article>`).join("")}</div>`;
}

function reportSectionContext(iconName: string, now: string, next: string): string {
  return `<div class="report-context"><span>${dashboardIcon(iconName)}</span><div><small>NOW / 今の状態</small><b>${escapeHtml(now)}</b></div><div><small>NEXT / 次に見ること</small><b>${escapeHtml(next)}</b></div></div>`;
}

export function renderImprovementReport(data?: ThreadsDashboardData): string {
  const connected = data?.connected === true;
  const contents = connected ? data.contents.slice(0, 12) : [];
  const measured = contents.filter(hasMetrics);
  const published = contents.filter((content) => Boolean(content.publication?.externalId));
  const sortedMeasured = [...measured].sort((a, b) => (b.metricsObservedAt || "").localeCompare(a.metricsObservedAt || ""));
  const latest = sortedMeasured[0] ?? null;
  const latestAt = sortedMeasured.map((content) => content.metricsFetchedAt ?? content.metricsObservedAt).filter((value): value is string => Boolean(value)).sort().at(-1) ?? null;
  const summary = data?.editorial.customerSummary ?? null;
  const registeredTheme = customerText(summary, ["current_theme", "theme", "improvement_theme", "focus"]);
  const registeredVariable = customerText(summary, ["test_variable", "variable", "changed_variable"]);
  const registeredNext = customerText(summary, ["next_test", "next_action", "next_change"]);
  const registeredReason = customerText(summary, ["reason", "rationale", "why"]);
  const suggestedNext = measured.length >= 2
    ? "次回の新規投稿で、末尾に短い問いかけを入れるかだけを変更"
    : "未登録";
  const suggestedConstants = measured.length >= 2
    ? "topic・role・length帯・投稿時間帯（既存#2〜#5は変更しない）"
    : "未登録";
  const suggestedReason = measured.length >= 2
    ? "取得済み投稿の返信が0のため、返信の入口となる末尾要素を1変数で確認"
    : "理由はまだ登録されていません";
  const currentTheme = registeredTheme || latest?.topic || "改善テーマを特定できるデータがありません";
  const state = !connected ? "接続待ち" : measured.length < 2 ? "判断保留" : "観測中";
  const evidence = measured.length >= 4 ? "中" : measured.length >= 2 ? "限定的" : measured.length === 1 ? "初期" : "未評価";
  const replyKnown = measured.filter((content) => typeof content.metrics.replies === "number");
  const totalReplies = replyKnown.reduce((sum, content) => sum + (content.metrics.replies ?? 0), 0);
  const keyFinding = replyKnown.length
    ? `${replyKnown.length}投稿の返信は合計${totalReplies.toLocaleString("ja-JP")}件`
    : "返信データを確認中";
  const maxViews = Math.max(1, ...measured.map((content) => content.metrics.views ?? 0));
  const safePosts = JSON.stringify(contents.map((content) => ({
    topic: content.topic || "テーマ未設定", body: content.body, role: roleLabel(content.contentRole),
    published: fmtDate(content.publication?.publishedAt ?? null, "未公開"), fetched: fmtDate(content.metricsFetchedAt ?? content.metricsObservedAt),
    metrics: Object.fromEntries(Object.entries(content.metrics).filter((entry) => typeof entry[1] === "number")),
  }))).replace(/</g, "\\u003c");

  const factCards = measured.map((content) => `<article class="fact-post post-open" tabindex="0" role="button" data-post-index="${contents.indexOf(content)}"><div class="fact-post-head"><div><div class="fact-tags"><span class="role-chip">${escapeHtml(roleLabel(content.contentRole))}</span><span class="role-chip">${escapeHtml(originLabel(content.origin))}</span>${content.origin === "human_manual" ? `<span class="role-chip">${content.analyzeEnabled ? "分析に使う" : "分析対象外"}</span><span class="role-chip">${content.learnEnabled ? "改善学習に使う" : "改善学習には使わない"}</span>` : ""}</div><h3>${escapeHtml(content.topic || "テーマ未設定")}</h3><small>公開 ${escapeHtml(fmtDate(content.publication?.publishedAt ?? null, "時刻不明"))} · 取得 ${escapeHtml(fmtDate(content.metricsFetchedAt ?? content.metricsObservedAt))}</small></div><span class="open-detail">投稿詳細 →</span></div><div class="fact-kpis">${(["views", "likes", "replies", "reposts", "quotes"] as ThreadsMetricKey[]).map((key) => renderMetricChip(content, key)).join("")}</div><div class="fact-rate"><span>表示/時 <b>${content.viewsPerHour !== null ? content.viewsPerHour.toFixed(1) : "取得不可"}</b></span><i><em style="width:${typeof content.metrics.views === "number" ? Math.max(content.metrics.views > 0 ? 2 : 0, content.metrics.views / maxViews * 100).toFixed(1) : 0}%"></em></i></div></article>`).join("");

  const timeline = [...contents].sort((a, b) => {
    const ad = a.metricsObservedAt || a.publication?.publishedAt || a.createdAt || "";
    const bd = b.metricsObservedAt || b.publication?.publishedAt || b.createdAt || "";
    return bd.localeCompare(ad);
  }).slice(0, 10).map((content) => {
    const measuredNow = hasMetrics(content);
    const time = content.metricsObservedAt || content.publication?.publishedAt || content.createdAt;
    return `<li><time>${escapeHtml(fmtDate(time, "時刻不明"))}</time><i class="${measuredNow ? "measured" : "planned"}"></i><div><b>${escapeHtml(content.topic || "テーマ未設定")}</b><p>${measuredNow ? `実測を取得：表示 ${valueOf(content, "views")} / いいね ${valueOf(content, "likes")} / 返信 ${valueOf(content, "replies")}` : content.publication?.externalId ? "公開済み・KPI取得待ち" : "準備中・まだ結果はありません"}</p></div></li>`;
  }).join("");

  return `<div class="customer-dashboard improvement-page">
  <header class="customer-hero improvement-hero"><div class="hero-main"><a class="back-link" href="/">← 運用サマリー</a><div class="page-kicker">AI IMPROVEMENT REPORT</div><h1>改善の理由と結果を、事実から追う</h1><p>試したこと、起きたこと、解釈、次の検証を分けて表示します。</p></div><div class="hero-actions"><div class="hero-update"><span>最新観測</span><b>${escapeHtml(fmtDate(latestAt, connected ? "KPI取得待ち" : "未接続"))}</b><small>Bridgeの実データのみ</small></div><button class="theme-toggle" id="theme-toggle" type="button" aria-label="表示テーマを切り替える"><span class="theme-moon">${dashboardIcon("moon")}</span><span class="theme-sun">${dashboardIcon("sun")}</span><span id="theme-label">ダーク</span></button></div></header>
  <nav class="report-tabs" aria-label="改善レポート内メニュー"><a href="#overview-report">改善テーマ</a><a href="#evidence">事実</a><a href="#comparison">比較</a><a href="#hypotheses">仮説</a><a href="#unknowns">判断保留</a><a href="#experiments">実験</a><a href="#next-test">次回テスト</a><a href="#history">履歴</a></nav>
  <section id="overview-report" class="report-panel report-chapter chapter-theme overview-panel" aria-label="改善サマリー"><div class="section-title"><div><span>01 · IMPROVEMENT THEME</span><h2>改善テーマ</h2><p>今何を検証しているかと、その判断に使える証拠の強さを見る場所です。</p></div><span class="state-chip">${escapeHtml(state)}</span></div>${reportSectionContext("experiment", `${state} · エビデンス ${evidence}`, registeredNext || suggestedNext)}<div class="theme-focus"><div><small>現在の改善テーマ</small><h3>${escapeHtml(currentTheme)}</h3><p>${registeredVariable ? `今回のテスト変数：${escapeHtml(registeredVariable)}` : "テスト変数はBridgeに登録されていません。投稿結果を観測し、判断を保留しています。"}</p></div><span>${dashboardIcon("experiment")}</span></div><div class="summary-strip"><div><small>対象投稿</small><b>${connected ? contents.length : "—"}</b></div><div><small>実KPI取得済み</small><b>${connected ? measured.length : "—"}</b></div><div><small>比較可能</small><b>${measured.length >= 2 ? `${measured.length}件` : "不足"}</b></div><div><small>エビデンス</small><b>${evidence}</b></div><div><small>最新観測</small><b>${escapeHtml(fmtDate(latestAt))}</b></div><div class="next-experiment-summary"><small>次回テスト</small><b>${escapeHtml(registeredNext || suggestedNext)}</b></div></div><div class="key-finding"><span>${dashboardIcon("fact")}</span><div><small>確認できた要点</small><b>${escapeHtml(keyFinding)}</b><p>${replyKnown.length && totalReplies === 0 ? "末尾の余白が反応に影響している可能性を、次回の1変数テストで確認します。" : "実測値を蓄積し、事実と仮説を分けて次の判断につなげます。"}</p></div></div><div class="evidence-note"><span>${dashboardIcon("unknown")}</span><p><b>断定しない設計です。</b> 投稿数が少ない間は「勝ち・負け」を表示せず、観測事実と仮説を分離します。</p></div></section>
  <section id="evidence" class="report-panel report-chapter chapter-evidence"><div class="section-title"><div><span>02 · EVIDENCE</span><h2>確認できた事実</h2><p>解釈を加える前に、取得済みのKPIだけを確認する場所です。</p></div><span class="count-pill">${measured.length}件</span></div>${reportSectionContext("fact", `実KPI取得 ${measured.length}件 · 最新 ${fmtDate(latestAt)}`, measured.length >= 2 ? "取得条件を揃えて比較へ" : "次の実測値が届くまで計測")}${factCards || `<div class="report-empty">${dashboardIcon("metrics")}<div><b>取得済みKPIはありません</b><p>実測値が届くまで、推定値や補完値は表示しません。</p></div></div>`}</section>
  <section id="comparison" class="report-panel report-chapter chapter-comparison comparison-panel" aria-label="投稿比較"><div class="section-title"><div><span>03 · KPI COMPARISON</span><h2>比較</h2><p>投稿ごとの違いから、次に検証する候補を絞るための場所です。</p></div><span class="count-pill">${measured.length >= 2 ? `${measured.length}件を比較` : "データ不足"}</span></div>${reportSectionContext("chart", measured.length >= 2 ? `${measured.length}件を同じ指標で比較中` : "比較データ待ち", measured.length >= 2 ? "差が再現するか次の投稿で確認" : "2件目の実KPI取得後に比較")}${renderComparisonBars(sortedMeasured)}${measured.length >= 2 ? `<details class="comparison-details"><summary>数値表を詳しく見る</summary><div class="comparison-table-wrap"><table class="comparison-table"><thead><tr><th>投稿</th><th>作成</th><th>役割</th><th>公開</th><th>表示</th><th>表示/時</th><th>いいね/表示</th><th>返信/表示</th><th>反応/時</th></tr></thead><tbody>${sortedMeasured.map((content) => `<tr class="post-open" tabindex="0" data-post-index="${contents.indexOf(content)}"><th>${escapeHtml(content.topic || "テーマ未設定")}</th><td>${escapeHtml(originLabel(content.origin))}</td><td>${escapeHtml(roleLabel(content.contentRole))}</td><td>${escapeHtml(fmtDate(content.publication?.publishedAt ?? null, "取得不可"))}</td><td>${valueOf(content, "views")}</td><td>${content.viewsPerHour !== null ? content.viewsPerHour.toFixed(1) : "取得不可"}</td><td>${ratio(content, "likes")}</td><td>${ratio(content, "replies")}</td><td>${content.engagementPerHour !== null ? content.engagementPerHour.toFixed(1) : "取得不可"}</td></tr>`).join("")}</tbody></table></div></details>` : ""}</section>
  <section id="hypotheses" class="report-panel report-chapter chapter-hypotheses"><div class="section-title"><div><span>04 · HYPOTHESES</span><h2>現時点の仮説</h2><p>事実から説明できそうなことを、反証条件つきで整理する場所です。</p></div><span class="state-chip muted">最大3件</span></div>${reportSectionContext("hypothesis", measured.length ? `${Math.min(measured.length >= 2 ? 2 : 1, 3)}件の仮説を検討中` : "仮説を作れる実測値待ち", "不足データを1変数テストで補う")}${renderHypotheses(measured)}</section>
  <section id="unknowns" class="report-panel report-chapter chapter-unknown unknown-panel"><div class="section-title"><div><span>05 · UNKNOWNS</span><h2>まだ判断できないこと</h2><p>証拠が足りず、現時点では結論にしてはいけない項目を確認する場所です。</p></div><span class="state-chip">注意 · 小標本</span></div>${reportSectionContext("unknown", "比較条件と再現性が不足", "追加計測と条件を揃えた投稿で確認")}<div class="unknown-grid">${[
    ["時間帯の影響", "同じ内容を近い条件で比較していません。"], ["topicの良し悪し", "テーマ以外の差を固定した検証が不足しています。"],
    ["endingの影響", "末尾だけを変えた対照投稿がありません。"], ["reaction lag", "返信や再投稿は遅れて増える可能性があります。"],
    ["再現性", `実KPI取得は${measured.length}件で、一般化には小標本です。`],
  ].map(([title, body]) => `<article><span>${dashboardIcon("unknown")}</span><div><b>${title}</b><p>${body}</p></div></article>`).join("")}</div></section>
  <section id="experiments" class="report-panel report-chapter chapter-experiment" aria-label="今回試したこと / 次回テスト"><div class="section-title"><div><span>06 · EXPERIMENT</span><h2>実験</h2><p>今回変えた要素と、固定した条件、実験の進行状態を見る場所です。</p></div><span class="state-chip muted">${data?.editorial.experiments.length ? "記録あり" : "記録待ち"}</span></div>${reportSectionContext("experiment", data?.editorial.experiments.length ? `${data.editorial.experiments.length}件の実験記録あり` : "構造化された実験記録待ち", "観測結果を次回テストへ反映")}${data?.editorial.experiments.length ? `<div class="experiment-safe"><span>${dashboardIcon("experiment")}</span><div><b>実験データを取得済み</b><p>内部記録は顧客画面へそのまま転記せず、顧客向けサマリーに登録された項目だけを表示します。</p></div></div>` : `<div class="report-empty compact"><div><b>構造化された実験記録はまだ取得できません</b><p>現在のBridgeから実験変数・固定要素・成功条件を取得できないため、推測で埋めていません。</p></div></div>`}</section><section id="next-test" class="report-panel report-chapter chapter-next"><div class="section-title"><div><span>07 · NEXT TEST</span><h2>次回テスト</h2><p>次に何を1つだけ変えるかと、その理由を確認する場所です。</p></div><span class="state-chip muted">1変数</span></div>${reportSectionContext("next", registeredNext || suggestedNext, measured.length >= 2 ? "次回の新規投稿で実行" : "比較可能な実績が揃い次第決定")}<div class="next-test-card"><div class="one-variable"><span>1</span><div><small>原則</small><b>一度に変える変数は1つ</b></div></div><div class="next-test-grid"><div><small>次回変えること</small><b>${escapeHtml(registeredNext || suggestedNext)}</b></div><div><small>今回維持すること</small><b>${escapeHtml(registeredVariable ? "登録済み変数以外" : suggestedConstants)}</b></div><div><small>このテストを選ぶ理由</small><b>${escapeHtml(registeredReason || suggestedReason)}</b></div></div></div></section>
  <section id="history" class="report-panel report-chapter chapter-history"><div class="section-title"><div><span>08 · HISTORY</span><h2>改善履歴</h2><p>過去の投稿・公開・観測をたどり、改善がどう進んだかを見る場所です。</p></div><span class="state-chip muted">${contents.length}件</span></div>${reportSectionContext("clock", `${contents.length}件の出来事を時系列表示`, "次回の投稿・KPI取得時に更新")}${timeline ? `<ol class="report-timeline">${timeline}</ol>` : `<div class="report-empty compact"><div><b>表示できる履歴がありません</b><p>投稿データの接続後に時系列で表示します。</p></div></div>`}</section>
  </div>
  <div class="drawer-backdrop report-backdrop" id="report-backdrop" hidden></div><aside class="post-drawer report-drawer" id="report-drawer" aria-hidden="true"><button class="drawer-close" id="report-close" type="button" aria-label="閉じる">×</button><div class="card-eyebrow">POST DETAIL</div><h2 id="report-post-title"></h2><div class="drawer-grid"><div><span>投稿の役割</span><p id="report-post-role"></p></div><div><span>公開時刻</span><p id="report-post-published"></p></div><div><span>最終取得</span><p id="report-post-fetched"></p></div></div><div class="drawer-block"><span>投稿本文</span><p id="report-post-body"></p></div><div class="drawer-block"><span>取得済みKPI</span><p id="report-post-metrics"></p></div></aside>
  <style>${customerDashboardStyles}${reportStyles}</style><script>(()=>{${customerThemeScript}const posts=${safePosts};const drawer=document.getElementById('report-drawer'),backdrop=document.getElementById('report-backdrop'),close=document.getElementById('report-close');function openPost(index){const post=posts[index];if(!post||!drawer||!backdrop)return;document.getElementById('report-post-title').textContent=post.topic;document.getElementById('report-post-role').textContent=post.role;document.getElementById('report-post-published').textContent=post.published;document.getElementById('report-post-fetched').textContent=post.fetched;document.getElementById('report-post-body').textContent=post.body;const metrics=Object.entries(post.metrics).map(([key,value])=>({views:'表示',likes:'いいね',replies:'返信',reposts:'再投稿',quotes:'引用',shares:'シェア'}[key]||key)+' '+Number(value).toLocaleString('ja-JP')).join(' / ');document.getElementById('report-post-metrics').textContent=metrics||'取得不可';backdrop.hidden=false;drawer.classList.add('open');drawer.setAttribute('aria-hidden','false')}function closePost(){drawer?.classList.remove('open');drawer?.setAttribute('aria-hidden','true');if(backdrop)backdrop.hidden=true}document.querySelectorAll('.post-open').forEach(row=>{row.addEventListener('click',()=>openPost(Number(row.dataset.postIndex)));row.addEventListener('keydown',event=>{if(event.key==='Enter'||event.key===' '){event.preventDefault();openPost(Number(row.dataset.postIndex))}})});close?.addEventListener('click',closePost);backdrop?.addEventListener('click',closePost);document.addEventListener('keydown',event=>{if(event.key==='Escape')closePost()})})();</script>`;
}

const reportStyles = `
.fact-tags{display:flex;gap:5px;flex-wrap:wrap}
.improvement-page{max-width:1440px;margin:0 auto}.improvement-hero{display:flex;justify-content:space-between;gap:24px}.improvement-hero .hero-update{min-width:190px;padding:13px 15px;border:1px solid var(--c-line);border-radius:14px;display:grid}.improvement-hero .hero-update span,.improvement-hero .hero-update small{font-size:9px;color:var(--c-faint)}.improvement-hero .hero-update b{margin:2px 0;font-size:12px}.back-link{display:block;width:max-content;margin-bottom:12px;color:var(--c-brand);font-size:10px;font-weight:800;text-decoration:none}.report-tabs{position:sticky;top:8px;z-index:8;display:flex;gap:5px;overflow-x:auto;margin:-3px 0 14px;padding:7px;border:1px solid var(--c-line);border-radius:14px;background:color-mix(in srgb,var(--c-panel) 92%,transparent);box-shadow:var(--c-shadow);backdrop-filter:blur(16px)}.report-tabs a{padding:8px 11px;border-radius:9px;color:var(--c-muted);font-size:9px;font-weight:800;text-decoration:none;white-space:nowrap}.report-tabs a:hover{background:var(--c-panel-soft);color:var(--c-brand)}.report-panel{scroll-margin-top:80px;margin-bottom:14px;padding:22px;border:1px solid var(--c-line);border-radius:18px;background:var(--c-panel);box-shadow:var(--c-shadow)}.section-title{display:flex;align-items:flex-start;justify-content:space-between;gap:16px;margin-bottom:16px}.section-title>div>span{display:block;margin-bottom:5px;color:var(--c-purple);font-size:8px;font-weight:800;letter-spacing:.15em}.section-title h2{margin:0;color:var(--c-text);font-size:19px;letter-spacing:-.02em}.section-title p{margin-top:4px;color:var(--c-muted);font-size:10px}.state-chip{padding:6px 10px;border-radius:999px;background:color-mix(in srgb,var(--c-amber) 14%,var(--c-panel));color:var(--c-amber);font-size:9px;font-weight:800;white-space:nowrap}.state-chip.muted{background:var(--c-panel-soft);color:var(--c-muted)}.theme-focus{display:flex;align-items:center;justify-content:space-between;gap:20px;padding:22px;border-radius:16px;background:linear-gradient(135deg,#172554,#312e81);color:#fff}.theme-focus small{color:#bfdbfe;font-size:9px;font-weight:800}.theme-focus h3{margin:5px 0;color:#fff;font-size:23px;letter-spacing:-.03em}.theme-focus p{color:#dbeafe;font-size:10px}.theme-focus>span{display:grid;width:52px;height:52px;place-items:center;border-radius:15px;background:rgba(255,255,255,.12);color:#c4b5fd}.theme-focus .ui-icon{width:25px;height:25px}.summary-strip{display:grid;grid-template-columns:repeat(5,1fr);gap:8px;margin-top:10px}.summary-strip>div{padding:13px;border:1px solid var(--c-line);border-radius:12px;background:var(--c-raised)}.summary-strip small{display:block;color:var(--c-faint);font-size:8px}.summary-strip b{display:block;margin-top:5px;color:var(--c-text);font-size:13px}.evidence-note{display:flex;gap:10px;align-items:center;margin-top:10px;padding:11px 13px;border-radius:12px;background:color-mix(in srgb,var(--c-amber) 8%,var(--c-panel));color:var(--c-amber)}.evidence-note p{color:var(--c-muted);font-size:9.5px}.evidence-note p b{color:var(--c-text-2)}.fact-post{padding:16px;border:1px solid var(--c-line);border-radius:14px;background:var(--c-raised);cursor:pointer}.fact-post+.fact-post{margin-top:9px}.fact-post:hover,.fact-post:focus{border-color:color-mix(in srgb,var(--c-brand) 48%,var(--c-line));outline:none}.fact-post-head{display:flex;justify-content:space-between;gap:12px}.fact-post h3{margin:6px 0 2px;color:var(--c-text);font-size:13px}.fact-post small{color:var(--c-faint);font-size:8.5px}.role-chip{padding:4px 7px;border-radius:7px;background:color-mix(in srgb,var(--c-brand) 11%,var(--c-panel));color:var(--c-brand);font-size:8px;font-weight:800}.open-detail{color:var(--c-brand);font-size:9px;font-weight:800}.fact-kpis{display:grid;grid-template-columns:repeat(5,1fr);gap:7px;margin-top:13px}.fact-chip{padding:10px;border-radius:10px;background:color-mix(in srgb,var(--c-green) 9%,var(--c-panel))}.fact-chip small,.fact-chip b{display:block}.fact-chip small{color:var(--c-green);font-size:7.5px}.fact-chip b{margin-top:3px;font-size:13px}.fact-chip.missing{background:var(--c-panel-soft)}.fact-chip.missing small,.fact-chip.missing b{color:var(--c-faint)}.fact-rate{display:grid;grid-template-columns:80px 1fr;gap:10px;align-items:center;margin-top:11px}.fact-rate span{color:var(--c-muted);font-size:8.5px}.fact-rate i,.comparison-line i{height:6px;border-radius:99px;background:var(--c-panel-soft);overflow:hidden}.fact-rate em,.comparison-line em{display:block;height:100%;border-radius:99px;background:linear-gradient(90deg,var(--c-brand),var(--c-purple))}.hypothesis-list{display:grid;gap:9px}.hypothesis-card{padding:16px;border:1px solid var(--c-line);border-radius:14px;background:var(--c-raised)}.hypothesis-head{display:flex;align-items:center;gap:10px}.hypothesis-head>span{display:grid;width:30px;height:30px;place-items:center;border-radius:10px;background:color-mix(in srgb,var(--c-purple) 12%,var(--c-panel));color:var(--c-purple);font-size:11px;font-weight:800}.hypothesis-head small{color:var(--c-faint);font-size:7.5px}.hypothesis-head h3{margin:1px 0;color:var(--c-text);font-size:12px}.hypothesis-head em{margin-left:auto;padding:5px 8px;border-radius:99px;background:var(--c-panel-soft);color:var(--c-muted);font-size:8px;font-style:normal;font-weight:800}.hypothesis-card dl{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-top:13px}.hypothesis-card dl>div{padding:10px;border-radius:10px;background:var(--c-panel-soft)}.hypothesis-card dt{color:var(--c-faint);font-size:7.5px;font-weight:800}.hypothesis-card dd{margin:5px 0 0;color:var(--c-text-2);font-size:9px;line-height:1.6}.unknown-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:8px}.unknown-grid article,.experiment-safe{display:flex;gap:10px;padding:13px;border:1px solid color-mix(in srgb,var(--c-amber) 26%,var(--c-line));border-radius:12px;background:color-mix(in srgb,var(--c-amber) 7%,var(--c-panel))}.unknown-grid article>span,.experiment-safe>span{color:var(--c-amber)}.unknown-grid b,.experiment-safe b{font-size:10px}.unknown-grid p,.experiment-safe p{margin-top:3px;color:var(--c-muted);font-size:9px;line-height:1.55}.report-empty{min-height:150px;display:flex;align-items:center;justify-content:center;gap:12px;text-align:left;border:1px dashed var(--c-line);border-radius:13px;background:var(--c-panel-soft);color:var(--c-muted)}.report-empty.compact{min-height:90px}.report-empty>.ui-icon{width:28px;height:28px;color:var(--c-faint)}.report-empty b{font-size:11px}.report-empty p{margin-top:4px;color:var(--c-faint);font-size:9px}.next-test-card{display:grid;grid-template-columns:220px 1fr;gap:10px;margin-top:10px}.one-variable{display:flex;align-items:center;gap:12px;padding:16px;border-radius:13px;background:linear-gradient(135deg,#172554,#1e3a8a);color:#fff}.one-variable>span{display:grid;width:38px;height:38px;place-items:center;border-radius:50%;background:rgba(255,255,255,.13);font-size:18px;font-weight:800}.one-variable small{display:block;color:#bfdbfe;font-size:8px}.one-variable b{color:#fff;font-size:11px}.next-test-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:8px}.next-test-grid>div{padding:13px;border:1px solid var(--c-line);border-radius:12px;background:var(--c-raised)}.next-test-grid small{display:block;color:var(--c-faint);font-size:8px}.next-test-grid b{display:block;margin-top:5px;font-size:9.5px;line-height:1.55}.comparison-bars{display:grid;grid-template-columns:repeat(2,1fr);gap:10px}.comparison-group{padding:13px;border:1px solid var(--c-line);border-radius:12px;background:var(--c-raised)}.comparison-group>strong{display:block;margin-bottom:9px;font-size:10px}.comparison-line{display:grid;grid-template-columns:90px 1fr 48px;gap:8px;align-items:center;margin-top:7px}.comparison-line>span{overflow:hidden;color:var(--c-muted);font-size:8px;text-overflow:ellipsis;white-space:nowrap}.comparison-line>b{text-align:right;font-size:8px}.comparison-table-wrap{overflow-x:auto;margin-top:12px}.comparison-table{width:100%;min-width:850px;border-collapse:collapse}.comparison-table th,.comparison-table td{padding:10px;border-bottom:1px solid var(--c-line-soft);color:var(--c-text-2);font-size:8.5px;text-align:left}.comparison-table thead th{color:var(--c-faint);font-size:7.5px}.comparison-table tbody tr{cursor:pointer}.comparison-table tbody tr:hover{background:var(--c-panel-soft)}.report-timeline{position:relative;list-style:none;margin:0;padding:0}.report-timeline:before{content:'';position:absolute;left:112px;top:8px;bottom:8px;width:1px;background:var(--c-line)}.report-timeline li{display:grid;grid-template-columns:96px 16px 1fr;gap:9px;align-items:flex-start;padding:8px 0}.report-timeline time{padding-top:3px;color:var(--c-faint);font-size:8px;text-align:right}.report-timeline i{position:relative;z-index:1;width:10px;height:10px;margin:3px;border:2px solid var(--c-panel);border-radius:50%;background:var(--c-faint);box-shadow:0 0 0 1px var(--c-line)}.report-timeline i.measured{background:var(--c-green)}.report-timeline i.planned{background:var(--c-brand)}.report-timeline b{font-size:10px}.report-timeline p{margin-top:3px;color:var(--c-muted);font-size:9px}.report-backdrop{position:fixed;inset:0;z-index:50;background:rgba(15,23,42,.32);backdrop-filter:blur(2px)}.report-drawer{position:fixed;right:0;top:0;bottom:0;z-index:51;width:min(500px,92vw);padding:32px;overflow:auto;background:var(--c-panel);color:var(--c-text);box-shadow:-20px 0 50px rgba(15,23,42,.2);transform:translateX(104%);transition:transform .24s ease}.report-drawer.open{transform:translateX(0)}.report-drawer .drawer-close{position:absolute;right:20px;top:18px;width:32px;height:32px;border:0;border-radius:50%;background:var(--c-panel-soft);color:var(--c-text);font-size:20px}.report-drawer>h2{margin:0 0 14px;color:var(--c-text);font-size:21px}.report-drawer .drawer-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:8px}.report-drawer .drawer-grid>div,.report-drawer .drawer-block{margin-top:9px;padding:13px;border-radius:11px;background:var(--c-panel-soft)}.report-drawer span{display:block;color:var(--c-faint);font-size:8px;font-weight:800}.report-drawer p{margin-top:5px;color:var(--c-text-2);font-size:10px;line-height:1.7;white-space:pre-line}
@media(max-width:1000px){.summary-strip{grid-template-columns:repeat(3,1fr)}.unknown-grid{grid-template-columns:repeat(2,1fr)}.next-test-card{grid-template-columns:1fr}.comparison-bars{grid-template-columns:1fr}}
@media(max-width:700px){.customer-shell{overflow-x:hidden}.customer-shell .app{display:block;padding:10px}.customer-shell .sidebar{position:static;width:100%;height:auto;padding:11px 13px;margin-bottom:8px;border-radius:15px;display:flex;flex-direction:row;align-items:center;justify-content:space-between}.customer-shell .sidebar-header{padding:0}.customer-shell .sidebar-header .brand{font-size:13px}.customer-shell .sidebar-header .brand-mark,.customer-shell .sidebar-header .brand-mark img{width:34px;height:34px}.customer-shell .sidebar-header .brand-sub,.customer-shell .sidebar-nav{display:none}.customer-shell .sidebar-footer{padding:0;margin:0}.customer-shell .sys-status-card{padding:7px 9px}.customer-shell .sys-status-label{display:none}.customer-shell .main{min-width:0;padding:12px 2px 24px}.improvement-page{min-width:0}.improvement-hero{flex-direction:column;padding:18px}.improvement-hero .hero-actions{width:100%}.improvement-hero .hero-update{min-width:0;flex:1}.report-tabs{max-width:100%}.report-panel{min-width:0;padding:17px}.section-title{flex-direction:column}.summary-strip,.fact-kpis,.next-test-grid{grid-template-columns:1fr 1fr}.summary-strip>div,.fact-chip{min-width:0}.hypothesis-card dl{grid-template-columns:1fr}.unknown-grid{grid-template-columns:1fr}.theme-focus h3{font-size:19px}.theme-focus>span{display:none}.report-timeline:before{left:12px}.report-timeline li{grid-template-columns:16px minmax(0,1fr)}.report-timeline time{grid-column:2;text-align:left;padding:0}.report-timeline i{grid-column:1;grid-row:1 / span 2}.report-timeline li>div{grid-column:2}.report-drawer .drawer-grid{grid-template-columns:1fr}}
@media(max-width:430px){.summary-strip,.fact-kpis,.next-test-grid{grid-template-columns:1fr 1fr}.fact-post-head{display:block}.open-detail{display:block;margin-top:7px}.comparison-line{grid-template-columns:74px 1fr 44px}}

/* UX06: improvement report readability and visual-first flow */
.improvement-page{max-width:1380px}.improvement-hero{padding:30px 32px;margin-bottom:24px}.back-link{margin-bottom:16px;font-size:14px}.report-tabs{top:10px;gap:8px;margin:0 0 24px;padding:10px;border-radius:16px;scrollbar-width:none}.report-tabs::-webkit-scrollbar{display:none}.report-tabs a{padding:10px 14px;font-size:13px}
.report-panel{scroll-margin-top:96px;margin-bottom:24px;padding:32px;border-radius:20px}.section-title{gap:24px;margin-bottom:24px}.section-title>div>span{margin-bottom:8px;font-size:12px}.section-title h2{font-size:24px;line-height:1.35}.section-title p{margin-top:7px;font-size:14px;line-height:1.6}.state-chip{padding:8px 13px;font-size:13px}
.theme-focus{padding:30px;border-radius:18px}.theme-focus small{font-size:13px}.theme-focus h3{margin:8px 0;font-size:30px;line-height:1.35}.theme-focus p{font-size:14px;line-height:1.6}.theme-focus>span{width:68px;height:68px}.theme-focus .ui-icon{width:34px;height:34px}
.summary-strip{grid-template-columns:repeat(4,minmax(0,1fr));gap:14px;margin-top:16px}.summary-strip>div{min-height:108px;padding:18px;border-radius:14px}.summary-strip small{font-size:13px}.summary-strip b{margin-top:9px;font-size:22px;line-height:1.4}.summary-strip .next-experiment-summary{grid-column:span 2;background:color-mix(in srgb,var(--c-brand) 8%,var(--c-panel));border-color:color-mix(in srgb,var(--c-brand) 25%,var(--c-line))}.summary-strip .next-experiment-summary b{font-size:17px}
.key-finding{display:flex;align-items:flex-start;gap:18px;margin-top:16px;padding:24px;border-radius:16px;background:color-mix(in srgb,var(--c-green) 9%,var(--c-panel));border:1px solid color-mix(in srgb,var(--c-green) 22%,var(--c-line))}.key-finding>span{display:grid;width:50px;height:50px;place-items:center;flex:none;border-radius:14px;background:color-mix(in srgb,var(--c-green) 14%,var(--c-panel));color:var(--c-green)}.key-finding small{display:block;color:var(--c-green);font-size:13px;font-weight:800}.key-finding b{display:block;margin-top:4px;font-size:26px;line-height:1.35}.key-finding p{margin-top:6px;color:var(--c-muted);font-size:14px;line-height:1.65}.evidence-note{gap:14px;margin-top:16px;padding:16px 18px}.evidence-note p{font-size:14px;line-height:1.6}
.fact-post{padding:24px;border-radius:17px}.fact-post+.fact-post{margin-top:14px}.fact-post h3{margin:10px 0 4px;font-size:18px;line-height:1.45}.fact-post small{font-size:13px}.role-chip{padding:6px 9px;font-size:12px}.open-detail{font-size:13px}.fact-kpis{grid-template-columns:repeat(5,minmax(0,1fr));gap:11px;margin-top:20px}.fact-chip{padding:15px;border-radius:12px}.fact-chip small{font-size:12px}.fact-chip b{margin-top:5px;font-size:21px}.fact-rate{grid-template-columns:130px 1fr;gap:14px;margin-top:16px}.fact-rate span{font-size:13px}.fact-rate i,.comparison-line i{height:12px}
.hypothesis-list{gap:14px}.hypothesis-card{padding:24px;border-radius:17px}.hypothesis-head{gap:14px}.hypothesis-head>span{width:44px;height:44px;font-size:15px}.hypothesis-head small{font-size:12px}.hypothesis-head h3{font-size:18px;line-height:1.45}.hypothesis-head em{padding:7px 10px;font-size:12px}.hypothesis-card dl{gap:12px;margin-top:18px}.hypothesis-card dl>div{padding:16px}.hypothesis-card dt{font-size:12px}.hypothesis-card dd{margin-top:7px;font-size:14px;line-height:1.65}
.unknown-grid{grid-template-columns:repeat(2,minmax(0,1fr));gap:14px}.unknown-grid article,.experiment-safe{gap:14px;padding:20px;border-radius:15px}.unknown-grid article>span,.experiment-safe>span{width:28px;height:28px}.unknown-grid b,.experiment-safe b{font-size:16px}.unknown-grid p,.experiment-safe p{margin-top:6px;font-size:14px;line-height:1.65}
.report-empty{min-height:220px;gap:18px;padding:28px}.report-empty.compact{min-height:140px}.report-empty>.ui-icon{width:44px;height:44px}.report-empty b{font-size:18px}.report-empty p{margin-top:6px;font-size:14px;line-height:1.6}
.next-test-card{grid-template-columns:270px 1fr;gap:14px;margin-top:16px}.one-variable{gap:16px;padding:24px;border-radius:15px}.one-variable>span{width:50px;height:50px;font-size:24px}.one-variable small{font-size:12px}.one-variable b{font-size:16px}.next-test-grid{gap:12px}.next-test-grid>div{padding:18px}.next-test-grid small{font-size:12px}.next-test-grid b{margin-top:7px;font-size:14px;line-height:1.65}
.comparison-panel{background:linear-gradient(145deg,var(--c-panel),color-mix(in srgb,var(--c-brand) 4%,var(--c-panel)))}.comparison-bars{gap:16px}.comparison-group{padding:20px;border-radius:15px}.comparison-group>strong{margin-bottom:16px;font-size:17px}.comparison-line{grid-template-columns:minmax(120px,180px) 1fr 72px;gap:13px;margin-top:13px}.comparison-line>span{font-size:13px}.comparison-line>b{font-size:13px}.comparison-details{margin-top:20px}.comparison-details>summary{width:max-content;padding:10px 14px;border-radius:10px;background:var(--c-panel-soft);color:var(--c-brand);font-size:13px;font-weight:800;cursor:pointer}.comparison-table-wrap{margin-top:12px}.comparison-table th,.comparison-table td{padding:14px 12px;font-size:13px}.comparison-table thead th{font-size:12px}
.report-timeline:before{left:142px}.report-timeline li{grid-template-columns:126px 18px 1fr;gap:12px;padding:13px 0}.report-timeline time{font-size:13px}.report-timeline i{width:12px;height:12px}.report-timeline b{font-size:16px}.report-timeline p{margin-top:5px;font-size:14px;line-height:1.6}.report-drawer span{font-size:12px}.report-drawer p{font-size:14px}
@media(max-width:1000px){.summary-strip{grid-template-columns:repeat(2,minmax(0,1fr))}.next-test-card{grid-template-columns:1fr}.fact-kpis{grid-template-columns:repeat(3,minmax(0,1fr))}}
@media(max-width:700px){.improvement-hero{padding:20px}.report-panel{padding:20px}.theme-focus{padding:22px}.theme-focus h3{font-size:24px}.summary-strip{grid-template-columns:1fr 1fr}.summary-strip .next-experiment-summary{grid-column:span 2}.key-finding{padding:20px}.key-finding b{font-size:21px}.comparison-bars{grid-template-columns:1fr}.comparison-line{grid-template-columns:90px 1fr 62px}.fact-kpis{grid-template-columns:1fr 1fr}.unknown-grid{grid-template-columns:1fr}.report-timeline:before{left:12px}.report-timeline li{grid-template-columns:16px minmax(0,1fr)}}
@media(max-width:430px){.summary-strip{grid-template-columns:1fr}.summary-strip .next-experiment-summary{grid-column:auto}.key-finding{display:block}.key-finding>span{margin-bottom:14px}.fact-kpis,.next-test-grid{grid-template-columns:1fr}.comparison-line{grid-template-columns:78px 1fr 55px}.theme-focus h3{font-size:21px}}

/* UX07: one readable type system across the full improvement report */
.improvement-page{font-size:16px;line-height:1.6}.improvement-page .hero-main h1{font-size:40px}.report-tabs a{font-size:14px}.section-title>div>span{font-size:13px}.section-title h2{font-size:27px}.section-title p{font-size:15px}.state-chip{font-size:14px}.theme-focus small,.summary-strip small,.key-finding small,.fact-post small,.role-chip,.open-detail,.fact-chip small,.hypothesis-head small,.hypothesis-head em,.hypothesis-card dt,.one-variable small,.next-test-grid small{font-size:14px}.theme-focus p,.key-finding p,.evidence-note p,.hypothesis-card dd,.unknown-grid p,.experiment-safe p,.report-empty p,.next-test-grid b{font-size:15px}.summary-strip b{font-size:24px}.summary-strip .next-experiment-summary b{font-size:18px}.key-finding b{font-size:28px}.fact-post h3,.hypothesis-head h3,.comparison-group>strong{font-size:19px}.fact-chip{padding:17px}.fact-chip b{font-size:23px}.fact-rate span,.comparison-line>span,.comparison-line>b,.comparison-details>summary,.comparison-table th,.comparison-table td{font-size:14px}.comparison-table thead th{font-size:13px}.unknown-grid b,.experiment-safe b,.one-variable b{font-size:17px}.report-timeline time{font-size:14px}.report-timeline b{font-size:17px}.report-timeline p{font-size:15px}.report-drawer span{font-size:14px}.report-drawer p{font-size:15px}.report-drawer>h2{font-size:25px}
@media(max-width:700px){.improvement-page .hero-main h1{font-size:32px}.section-title h2{font-size:25px}.theme-focus h3{font-size:25px}.key-finding b{font-size:23px}}
/* UX08: eight distinct report chapters with explicit status and next step */
.report-chapter{--report-accent:var(--c-brand);position:relative;margin-bottom:34px;border-color:color-mix(in srgb,var(--report-accent) 22%,var(--c-line));background:linear-gradient(145deg,color-mix(in srgb,var(--report-accent) 7%,var(--c-panel)),var(--c-panel) 46%);box-shadow:0 18px 44px color-mix(in srgb,var(--report-accent) 8%,transparent)}.report-chapter:before{content:'';position:absolute;top:-18px;left:32px;right:32px;height:1px;background:linear-gradient(90deg,transparent,color-mix(in srgb,var(--report-accent) 48%,var(--c-line)),transparent)}.report-chapter:first-of-type:before{display:none}
.chapter-theme{--report-accent:#4f46e5}.chapter-evidence{--report-accent:#059669}.chapter-comparison{--report-accent:#0284c7}.chapter-hypotheses{--report-accent:#7c3aed}.chapter-unknown{--report-accent:#d97706}.chapter-experiment{--report-accent:#db2777}.chapter-next{--report-accent:#2563eb}.chapter-history{--report-accent:#64748b}
.report-chapter .section-title>div>span{color:var(--report-accent)}.report-context{display:grid;grid-template-columns:46px minmax(0,1fr) minmax(0,1fr);gap:14px;align-items:stretch;margin:-4px 0 24px;padding:15px;border:1px solid color-mix(in srgb,var(--report-accent) 18%,var(--c-line));border-radius:16px;background:color-mix(in srgb,var(--c-panel) 82%,transparent)}.report-context>span{display:grid;width:46px;height:46px;place-items:center;align-self:center;border-radius:14px;background:color-mix(in srgb,var(--report-accent) 12%,var(--c-panel));color:var(--report-accent)}.report-context>span .ui-icon{width:23px;height:23px}.report-context>div{display:flex;min-width:0;flex-direction:column;justify-content:center;padding:2px 14px;border-left:1px solid var(--c-line-soft)}.report-context small{font-size:13px;font-weight:800;letter-spacing:.05em;color:var(--report-accent)}.report-context b{margin-top:4px;color:var(--c-text-2);font-size:15px;line-height:1.5}
html[data-customer-theme="dark"] .chapter-theme{--report-accent:#818cf8}html[data-customer-theme="dark"] .chapter-evidence{--report-accent:#34d399}html[data-customer-theme="dark"] .chapter-comparison{--report-accent:#38bdf8}html[data-customer-theme="dark"] .chapter-hypotheses{--report-accent:#a78bfa}html[data-customer-theme="dark"] .chapter-unknown{--report-accent:#fbbf24}html[data-customer-theme="dark"] .chapter-experiment{--report-accent:#f472b6}html[data-customer-theme="dark"] .chapter-next{--report-accent:#60a5fa}html[data-customer-theme="dark"] .chapter-history{--report-accent:#cbd5e1}
@media(max-width:700px){.report-chapter{margin-bottom:28px}.report-chapter:before{left:20px;right:20px}.report-context{grid-template-columns:40px 1fr;gap:10px}.report-context>span{width:40px;height:40px}.report-context>div{padding:2px 9px}.report-context>div:last-child{grid-column:2}.report-context small{font-size:12px}.report-context b{font-size:14px}}
`;
